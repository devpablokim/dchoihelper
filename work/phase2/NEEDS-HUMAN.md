# Phase 2 잔여 사람 개입 항목
1. P4 모션 본빌드(Phase 4) 선행 조건: sudo apt install pkg-config libpango1.0-dev libcairo2-dev
   + pipx install manim (docs/doctor-env.md motion 프로파일). PoC는 blocked로 종결 — 덱 E2E에는 지장 없음.
